import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  conductTitle: {
    color: 'blue',
    margin:15
  },
  conduct: {
    fontSize:40,
    textAlign: 'center',
  },
  closedDescription: {
    height: 0,
  },
  openDescription: {
    height: 'auto'
  }
});